[package]
name = "assembly_wind_tunnel"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Wind-tunnel scenarios for assembly flattening at placement scale"
readme = "README.md"
keywords = ["benchmark", "geometry"]
categories = ["development-tools::profiling"]
publish = false

[lints]
workspace = true

[dependencies]
exedra_assembly = { features = ["std"], workspace = true }
exedra_constructive = { features = ["std"], workspace = true }
exedra_mesh = { features = ["std"], workspace = true }
exedra_primitives = { features = ["std"], workspace = true }
