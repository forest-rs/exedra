[package]
name = "exedra_mesh_build_bench"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Scaling benchmark for exedra_mesh construction from many open patches"
keywords = ["benchmark", "mesh", "half-edge"]
categories = ["algorithms"]

[lints]
workspace = true

[dependencies]
exedra_mesh = { features = ["std"], workspace = true }
