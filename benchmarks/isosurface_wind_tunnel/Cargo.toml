# Copyright 2026 the Exedra Authors
# SPDX-License-Identifier: Apache-2.0 OR MIT

[package]
name = "isosurface_wind_tunnel"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Deterministic quality and reduction oracle for adaptive isosurface extraction"
readme = "README.md"
keywords = ["benchmark", "geometry"]
categories = ["development-tools::profiling"]
publish = false

[lints]
workspace = true

[dependencies]
exedra = { features = ["std"], workspace = true }
exedra_isosurface = { features = ["std"], workspace = true }
exedra_math = { features = ["std"], workspace = true }
exedra_qef = { features = ["std"], workspace = true }
exedra_spatial.workspace = true
