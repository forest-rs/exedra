# exedra_testkit

Deterministic test helpers for Exedra:

- fixture mesh generators (`fixtures`)
- TriMesh golden snapshot helpers (`golden`)
- topology/attribute/OBJ debug dumps (`dump`)

This crate is intended for tests and examples.
